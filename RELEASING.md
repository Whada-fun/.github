# 版本與發布治理 (Releasing)

> **狀態：草案 (DRAFT)** — 發布節奏與支援期限待各專案確認。

本文件規範 WHADA 各專案的版本號、發布流程與相依套件更新政策。

> ℹ️ 本檔名**不在** GitHub 可繼承的預設社群健康文件清單內，不會自動套用到其他 repo。
> 它是組織的**單一真實來源**；各專案請於自己的 `CONTRIBUTING.md` 或 `README.md` 連結至此。

---

## 1. 版本號規範

### 1.1 函式庫、SDK、工具

嚴格採用 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-TW/)：`MAJOR.MINOR.PATCH`

| 遞增位 | 時機 |
| --- | --- |
| `MAJOR` | 不相容的 API 變更 |
| `MINOR` | 向後相容的新功能 |
| `PATCH` | 向後相容的錯誤修正 |

- `1.0.0` 之前（`0.y.z`）視為不穩定，可在任何版本引入不相容變更。
- 預發布版本標記為 `1.2.0-rc.1`、`1.2.0-beta.2`。

### 1.2 持續部署的網站（WHADA 主站）

主站採**持續部署**，沒有面向使用者的版本號 —— 線上永遠只有一個版本。
可觀測的識別碼為：

| 識別碼 | 用途 |
| --- | --- |
| `build_id` | 每次 CI 建置的唯一識別，寫入頁面 meta 與事件維度，用於回溯「這筆數據來自哪次建置」 |
| `catalog_snapshot_id` | 該次建置所使用的 feed 快照，用於重現目錄狀態 |
| git commit SHA | 程式碼版本 |

因此本文件 §2.1 的「打標籤發布」流程**不適用於主站**，改用 §2.4。
標籤與 SemVer 僅適用於獨立的函式庫或工具（如可重用的 feed adapter crate）。

> **不相容變更在此的意義不同**：主站沒有存檔，但有兩項不得任意破壞的東西 ——
> **既有網址**（影響 SEO 與已被索引的頁面）與 **`localStorage` 的「最近玩過」資料結構**。
> 兩者變更都必須提供轉址或相容讀取，不得直接失效。

### 1.3 標籤

發布時在 git 打上標籤，格式為 `v` 前綴：`v1.4.2`。標籤一經推送**不得移動或刪除**。

---

## 2. 發布流程

### 2.1 一般發布

1. 確認 `main` 為綠燈，且所有預定納入的 PR 皆已合併
2. 更新 `CHANGELOG.md`，將 `Unreleased` 區塊改為新版本並填上日期
3. 更新專案內的版本號檔案（`package.json`、`ProjectSettings` 等）
4. 開立 release PR，經 `[TODO: 建議 1]` 位 Reviewer 核准後合併
5. 打標籤並推送：`git tag -a v1.4.2 -m "v1.4.2" && git push origin v1.4.2`
6. 建立 GitHub Release，內容取自 CHANGELOG 對應區塊
7. 依平台流程送審／部署
8. 於 `[TODO: 公告管道]` 公告

### 2.2 發布前檢查

- [ ] CI 全綠（建置、測試、lint）
- [ ] CHANGELOG 已更新且內容對使用者有意義（不是 commit 訊息的複製貼上）
- [ ] 版本號已同步更新於所有相關檔案
- [ ] 不相容變更已在 CHANGELOG 明確標示並附遷移說明
- [ ] 相依套件無已知的高風險漏洞
- [ ] 第三方素材與套件的授權清單已更新
- [ ] 回滾方式已確認可行

### 2.3 緊急修補 (Hotfix)

1. 從**發布標籤**切出 `hotfix/<簡述>` 分支（不是從 `main`）
2. 只修必要的部分，不夾帶其他變更
3. 遞增 `PATCH` 版本後發布
4. **務必**將修補合併回 `main`，避免下一版又出現同樣的問題

生產事故的緊急處理另見 [`GOVERNANCE.md` §4.4](./GOVERNANCE.md)。

### 2.4 主站持續部署

主站不打標籤、不做版本發布。部署由兩個來源觸發：

| 觸發 | 內容 |
| --- | --- |
| **每日排程** | 抓取供應商 feed → 驗證 → diff → canonical 去重 → 健康與發布狀態更新 → 建置 → 檢查 → 部署 |
| **合併到 `main`** | 程式碼或內容變更 → 建置 → 檢查 → 部署 |

部署前的 CI 必須全部通過，缺一不可：

- [ ] feed schema 驗證通過（欄位缺失、URL 格式、圖片可達）
- [ ] canonical 去重無異常膨脹（新增數量超出門檻須人工確認）
- [ ] 建置產物的檔案數在上限內
- [ ] 無死連結
- [ ] `canonical`、`hreflang`、`x-default` 正確
- [ ] sitemap 只包含 `index_basic` 以上的頁面
- [ ] `ads.txt` 存在於根路徑
- [ ] **無實質變更則不部署**

**回滾**：重新部署前一個成功的建置產物。因為輸出是純靜態資產，回滾等同於還原
上一版 artifact，不需要資料庫遷移。請確保 CI 保留至少 `[TODO: 建議 7 天]` 的建置產物。

> ⚠️ **供應商資料錯誤不得直接覆蓋人工內容。** feed 同步只能更新 `provider_variant`
> 層的欄位；`editorial_*` 與 `human_reviewed` 的本地化內容須明確保護。

---

## 3. CHANGELOG

採用 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/) 格式，
維護於各專案根目錄的 `CHANGELOG.md`。

分類使用：`Added`／`Changed`／`Deprecated`／`Removed`／`Fixed`／`Security`。

```markdown
## [Unreleased]

## [1.4.2] - 2026-08-01

### Fixed
- 修正 iOS 進入背景後音效無法恢復的問題 (#1234)

### Security
- 更新 xxx 至 2.1.4，修補 CVE-2026-XXXXX
```

原則：

- **寫給使用者看，不是寫給開發者看。** 描述影響，而非實作細節。
- 每則變更附上 issue 或 PR 編號。
- 不相容變更必須放在最顯眼處並標示 **BREAKING**。
- 純內部重構若對使用者無感，可以不列。

---

## 4. 網址與內容的淘汰

主站沒有「版本支援期限」，但**網址是長期資產**，淘汰需要規則：

| 情況 | 處理 |
| --- | --- |
| 遊戲 `suspended` | 頁面保留，移出 sitemap 與推薦，顯示替代遊戲，**不回 404** |
| 遊戲 `removed`（供應商下架） | 保留頁面並說明已下架 + 推薦替代，或 301 至該分類；**避免大量 404** |
| 分類或策展頁合併 | 301 至新網址，舊網址不得直接刪除 |
| 語言版本下線 | 移除該語言的 `hreflang`，301 至 `x-default` |

- 任何會改變既有網址的變更，**必須**在同一次部署內附上轉址規則。
- 大量 404 會直接傷害索引，屬於 `GOVERNANCE.md` §4.2 必須攔下的變更。

---

## 5. 相依套件更新政策

- 啟用 **Dependabot**（或等效工具）自動偵測過期與有漏洞的相依套件。
- 修補時限依嚴重程度：

  | 嚴重程度 | 修補時限 |
  | --- | --- |
  | Critical | `[TODO: 建議 7 天]` |
  | High | `[TODO: 建議 30 天]` |
  | Medium / Low | 下個排定版本 |

- 相依套件**必須鎖定版本**（lockfile 納入版控）。
- 引入新的相依套件前，確認其授權條款相容性，見 [`GOVERNANCE.md` §6](./GOVERNANCE.md)。
- 對外發布的專案應產出並保存 SBOM。

---

## 6. 相關文件

- [`CONTRIBUTING.md`](./CONTRIBUTING.md) —— 分支命名與 commit 規範
- [`GOVERNANCE.md`](./GOVERNANCE.md) —— 決策門檻與緊急變更流程
- [`SECURITY.md`](./SECURITY.md) —— 支援版本與漏洞修補時限
